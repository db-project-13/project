# Team13-Phase4 환경 변수 설정 예제 파일
# 
# 사용 방법:
# 1. 이 파일을 복사하여 .env 파일로 이름 변경
# 2. 아래 값들을 실제 환경에 맞게 수정
# 3. .env 파일은 Git에 커밋하지 않도록 주의 (.gitignore에 포함됨)

# Flask 환경 설정
FLASK_ENV=development
FLASK_DEBUG=True

# Flask Secret Key (프로덕션에서는 반드시 강력한 랜덤 문자열로 변경)
SECRET_KEY=dev-secret-key-change-in-production

# Oracle Database 연결 정보
# DSN 형식: host:port/service_name 또는 host:port:SID
DB_DSN=localhost:1521/orcl
DB_USER=your-db-username
DB_PASSWORD=your-db-password

# Connection Pool 설정
DB_POOL_MIN=2
DB_POOL_MAX=10
